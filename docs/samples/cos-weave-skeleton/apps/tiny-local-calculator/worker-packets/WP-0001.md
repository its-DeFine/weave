# Worker Packet WP-0001

App: Tiny Local Calculator (`tiny-local-calculator`)

Current lifecycle stage: `intent`

Use a visible pinned Codex worker when the host supports thread creation. If
that surface is unavailable, keep this packet as the local worker instruction.

Review loop: observe -> validate -> govern -> review -> sync.
